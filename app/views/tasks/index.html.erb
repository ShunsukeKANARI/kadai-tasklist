<h1>やること一覧</h1>

<ul>
    <% @task.each do |task| %>
        <li><%= link_to task.id, task %> : <%= task.title %> &gt; <%= task.content %></li>
    <% end %>
</ul>

<%= link_to '新規やること追加', new_task_path %>